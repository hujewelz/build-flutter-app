import 'package:flutter/material.dart';

class Palette {
  static const Color primary = Color(0xFF198CD8);
  static const Color separator = Color.fromARGB(15, 0, 0, 0);
  static const Color primaryText = Colors.black;
  static const Color secondaryText = Color.fromARGB(160, 0, 0, 0);
}
